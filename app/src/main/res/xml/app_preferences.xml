<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference
        android:key="auto_connect"
        android:title="@string/pref_title_auto_connect"
        android:summary="@string/pref_description_auto_connect"
        android:defaultValue="false" />

    <ListPreference
        android:key="default_device"
        android:dependency="auto_connect"
        android:title="@string/pref_title_default_device"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <ListPreference
        android:key="fade_steps"
        android:title="@string/pref_title_fade_steps"
        android:entries="@array/pref_fade_steps"
        android:entryValues="@array/pref_fade_step_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
